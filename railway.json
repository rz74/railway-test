{
    "build": {
      "root": "backend",
      "buildCommand": "pip install -r requirements.txt"
    },
    "start": {
      "root": "backend",
      "startCommand": "gunicorn app:app --bind 0.0.0.0:$PORT"
    }
  }
  