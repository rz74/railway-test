import React, { useState } from 'react';
import { BACKEND_URL } from '../path_config';

//... (rest of the content would be the same as before; shortened here for brevity)

export default ImageUploadGrid;
